<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Compression Threshold (bytes)
    |--------------------------------------------------------------------------
    | Images smaller than this are skipped during optimization.
    */
    'compress_threshold' => (int) env('IMAGE_COMPRESS_THRESHOLD', 204800),

    /*
    |--------------------------------------------------------------------------
    | JPEG / PNG Quality (1-100)
    |--------------------------------------------------------------------------
    */
    'quality' => (int) env('IMAGE_QUALITY', 82),

    /*
    |--------------------------------------------------------------------------
    | WebP Quality (1-100)
    |--------------------------------------------------------------------------
    */
    'webp_quality' => (int) env('IMAGE_WEBP_QUALITY', 80),

    /*
    |--------------------------------------------------------------------------
    | Max Width — images wider than this are resized down (px)
    |--------------------------------------------------------------------------
    */
    'max_width' => (int) env('IMAGE_MAX_WIDTH', 1200),

    /*
    |--------------------------------------------------------------------------
    | Srcset Thumbnail Widths (px)
    |--------------------------------------------------------------------------
    | Each entry generates a thumbnail at the given width.
    | Set to empty array to disable srcset generation.
    */
    'srcset_widths' => [400, 800],

    /*
    |--------------------------------------------------------------------------
    | Supported MIME Types
    |--------------------------------------------------------------------------
    */
    'supported_mimes' => [
        'image/jpeg',
        'image/png',
        'image/webp',
    ],
];
